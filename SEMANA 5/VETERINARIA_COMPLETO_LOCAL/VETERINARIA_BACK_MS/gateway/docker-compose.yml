services:
  nginx:
    image: nginx:latest
    container_name: gateway
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    networks: 
      - veterinaria-net # Conexión a la red externa 'tienda-net'

networks:
  veterinaria-net:
    external: true